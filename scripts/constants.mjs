const BASE = "https://api.noroff.dev/api/v1/social/";
const REGISTER = `${BASE}auth/register`;
const LOGIN = `${BASE}auth/login`;
const POSTS = `${BASE}posts`;
const USER_POSTS = `${BASE}profiles/`

export const API_URLS = {
    BASE,
    LOGIN,
    POSTS,
    USER_POSTS,
    REGISTER,
};